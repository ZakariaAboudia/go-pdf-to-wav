
# Installation

First clone the project :

```bash
    $ git clone https://github.com/ZakariaAboudia/go-pdf-to-wav.git
    $ cd go-pdf-to-wav
    $ wget https://github.com/rhasspy/piper/releases/download/2023.11.14-2/piper_linux_x86_64.tar.gz && tar -xvzf piper_linux_x86_64.tar.gz
    $ sudo docker compose up --build
```

# TODO:
- Improve the project's folder
- Present a better way to select pdf files
- Anything else that will be needed in the future (maybe)
